# 相册图片上传功能 - 部署检查清单

使用本清单确保所有配置步骤都已完成。

---

## 📋 第一阶段：本地开发环境

### 1. 环境准备
- [ ] Java 8+ 已安装 (`java -version`)
- [ ] Maven 已安装 (`mvn -version`)
- [ ] MySQL 8.0 已安装并运行 (`sudo systemctl status mysql`)
- [ ] Git 已安装 (`git --version`)

### 2. 数据库配置
- [ ] 执行建表脚本 (`mysql -u root -p < album-backend/database.sql`)
- [ ] 验证数据库创建成功 (`USE album_db; SHOW TABLES;`)
- [ ] 验证示例数据存在 (`SELECT * FROM album_image;`)

### 3. 阿里云 OSS 配置
- [ ] 创建 OSS Bucket（名称: wzy-album）
- [ ] 设置 Bucket 权限为"公共读"
- [ ] 配置 CORS 规则（允许前端域名）
- [ ] 创建 AccessKey（或 RAM 子账号）
- [ ] 保存 AccessKey ID 和 Secret

### 4. 后端配置
- [ ] 修改 `application.yml` 中的 MySQL 密码
- [ ] 修改 `application.yml` 中的 OSS AccessKey ID
- [ ] 修改 `application.yml` 中的 OSS AccessKey Secret
- [ ] 验证 Bucket 名称和 endpoint 正确

### 5. 后端编译和测试
- [ ] 编译项目 (`mvn clean package -DskipTests`)
- [ ] 启动后端 (`java -jar target/album-backend-1.0.0.jar`)
- [ ] 测试健康检查 (`curl http://localhost:8080/api/album/health`)
- [ ] 测试获取列表 (`curl http://localhost:8080/api/album/list`)
- [ ] 测试上传图片（使用 curl 或 Postman）

### 6. 前端配置和测试
- [ ] 确认 `album.js` 中的 API_BASE_URL 为 `http://localhost:8080/api/album`
- [ ] 使用 Live Server 或本地服务器打开 `album.html`
- [ ] 验证页面加载正常
- [ ] 验证图片轮播正常
- [ ] 测试上传功能
- [ ] 验证上传后图片出现在轮播中

---

## 🚀 第二阶段：生产环境部署

### 1. 服务器准备
- [ ] 购买或准备服务器（阿里云/腾讯云）
- [ ] 配置安全组（开放 80, 443, 8080 端口）
- [ ] 配置防火墙规则
- [ ] 更新系统 (`sudo apt update && sudo apt upgrade`)

### 2. 服务器环境安装
- [ ] 安装 Java 8+ (`sudo apt install openjdk-8-jdk`)
- [ ] 安装 MySQL 8.0 (`sudo apt install mysql-server`)
- [ ] 安装 Nginx (`sudo apt install nginx`)
- [ ] 配置 MySQL 安全设置 (`sudo mysql_secure_installation`)

### 3. 数据库部署
- [ ] 上传 `database.sql` 到服务器
- [ ] 执行建表脚本
- [ ] 创建数据库用户（如果需要）
- [ ] 配置远程访问权限（如果需要）
- [ ] 测试数据库连接

### 4. 后端部署
- [ ] 创建部署目录 (`sudo mkdir -p /opt/album`)
- [ ] 上传 jar 文件到服务器
- [ ] 创建生产环境配置文件 `application-prod.yml`
- [ ] 修改生产环境配置（数据库、OSS）
- [ ] 测试启动 (`java -jar album-backend-1.0.0.jar --spring.profiles.active=prod`)

### 5. Systemd 服务配置
- [ ] 复制 `album.service` 到 `/etc/systemd/system/`
- [ ] 修改服务文件中的路径
- [ ] 重载 systemd (`sudo systemctl daemon-reload`)
- [ ] 启动服务 (`sudo systemctl start album`)
- [ ] 设置开机自启 (`sudo systemctl enable album`)
- [ ] 验证服务状态 (`sudo systemctl status album`)
- [ ] 查看日志 (`sudo journalctl -u album -f`)

### 6. Nginx 配置
- [ ] 复制 `nginx.conf` 到 `/etc/nginx/sites-available/album`
- [ ] 修改配置中的域名或 IP
- [ ] 创建软链接 (`sudo ln -s /etc/nginx/sites-available/album /etc/nginx/sites-enabled/`)
- [ ] 测试配置 (`sudo nginx -t`)
- [ ] 重启 Nginx (`sudo systemctl restart nginx`)
- [ ] 测试反向代理 (`curl http://your-domain.com/api/album/health`)

### 7. HTTPS 配置（可选但推荐）
- [ ] 安装 Certbot (`sudo apt install certbot python3-certbot-nginx`)
- [ ] 申请证书 (`sudo certbot --nginx -d your-domain.com`)
- [ ] 验证自动续期 (`sudo certbot renew --dry-run`)
- [ ] 测试 HTTPS 访问

### 8. 前端部署
- [ ] 修改 `album.js` 中的 API_BASE_URL 为生产环境地址
- [ ] 提交代码到 Git (`git add . && git commit -m "Update API URL"`)
- [ ] 推送到 GitHub (`git push origin main`)
- [ ] 等待 GitHub Pages 部署完成
- [ ] 访问 `https://zygggggg.github.io/html/album.html`
- [ ] 验证功能正常

---

## 🧪 第三阶段：功能测试

### 1. 后端 API 测试
- [ ] 健康检查接口正常
- [ ] 获取图片列表接口正常
- [ ] 上传图片接口正常
- [ ] 删除图片接口正常
- [ ] 错误处理正常（上传非图片文件）
- [ ] 错误处理正常（上传超大文件）

### 2. 前端功能测试
- [ ] 页面加载正常
- [ ] 图片轮播正常
- [ ] 点击上传按钮打开弹窗
- [ ] 选择图片显示预览
- [ ] 上传成功后刷新列表
- [ ] 新上传的图片出现在轮播中
- [ ] 错误提示正常显示

### 3. 跨域测试
- [ ] 浏览器控制台无 CORS 错误
- [ ] API 请求成功（状态码 200）
- [ ] 响应头包含正确的 CORS 头

### 4. 兼容性测试
- [ ] Chrome 浏览器正常
- [ ] Firefox 浏览器正常
- [ ] Safari 浏览器正常
- [ ] Edge 浏览器正常
- [ ] 移动端浏览器正常

### 5. 性能测试
- [ ] 上传速度正常
- [ ] 图片加载速度正常
- [ ] 轮播切换流畅
- [ ] 并发上传测试通过

---

## 🔒 第四阶段：安全检查

### 1. 文件上传安全
- [ ] 文件类型验证生效
- [ ] 文件大小限制生效
- [ ] 文件名使用 UUID（无法猜测）
- [ ] 无法上传可执行文件

### 2. API 安全
- [ ] CORS 白名单配置正确
- [ ] 无敏感信息泄露
- [ ] 错误信息不暴露系统细节

### 3. 数据库安全
- [ ] 使用强密码
- [ ] 配置 IP 白名单
- [ ] 禁用 root 远程登录
- [ ] 定期备份配置完成

### 4. OSS 安全
- [ ] 使用 RAM 子账号（推荐）
- [ ] AccessKey 未泄露
- [ ] Bucket 权限配置正确
- [ ] 防盗链配置（可选）

### 5. 服务器安全
- [ ] 防火墙配置正确
- [ ] SSH 密钥登录（推荐）
- [ ] 禁用 root 登录（推荐）
- [ ] 定期更新系统

---

## 📊 第五阶段：监控和维护

### 1. 日志监控
- [ ] 配置日志轮转
- [ ] 定期查看应用日志
- [ ] 定期查看 Nginx 日志
- [ ] 定期查看 MySQL 日志

### 2. 性能监控
- [ ] 监控 CPU 使用率
- [ ] 监控内存使用率
- [ ] 监控磁盘空间
- [ ] 监控网络流量

### 3. 数据备份
- [ ] 配置数据库自动备份
- [ ] 测试备份恢复
- [ ] 配置 OSS 备份（可选）
- [ ] 定期验证备份完整性

### 4. 告警配置
- [ ] 配置服务异常告警
- [ ] 配置磁盘空间告警
- [ ] 配置数据库连接告警
- [ ] 配置 OSS 流量告警

---

## 📝 第六阶段：文档和交接

### 1. 文档完善
- [ ] 更新 README.md
- [ ] 记录配置信息（不含密码）
- [ ] 记录常见问题和解决方案
- [ ] 记录运维操作手册

### 2. 密码管理
- [ ] 记录所有密码（安全存储）
- [ ] 记录 AccessKey（安全存储）
- [ ] 记录服务器登录信息
- [ ] 配置密码定期更换提醒

### 3. 联系方式
- [ ] 记录阿里云账号信息
- [ ] 记录服务器提供商信息
- [ ] 记录域名注册商信息
- [ ] 记录技术支持联系方式

---

## ✅ 完成标志

当以上所有项目都打勾后，说明部署已完成！

### 最终验证
- [ ] 访问生产环境 URL，页面正常显示
- [ ] 上传一张图片，成功显示在轮播中
- [ ] 删除一张图片，成功从列表中移除
- [ ] 查看后端日志，无错误信息
- [ ] 查看 OSS 控制台，文件正常存储

---

## 🎉 部署完成！

恭喜！相册图片上传功能已成功部署。

### 下一步
1. 通知用户系统已上线
2. 收集用户反馈
3. 根据反馈优化功能
4. 规划下一阶段功能

---

## 📞 遇到问题？

1. 查看 `QUICK_START.md` - 快速开始指南
2. 查看 `DEPLOYMENT_GUIDE.md` - 完整部署指南
3. 查看 `IMPLEMENTATION_SUMMARY.md` - 实现总结
4. 查看后端日志: `sudo journalctl -u album -f`
5. 查看浏览器控制台: F12 → Console

---

**检查清单版本**: 1.0.0
**最后更新**: 2026-02-04
